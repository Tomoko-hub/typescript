export {};

type ObjectType = {
    name:string;
    level:number;
}

interface ObjectInterface {
    name: string;
    level: number;
}

let object:ObjectInterface = {
    name: 'Tomoko',
    level: 5
};
